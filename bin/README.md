# bin/

Windows 可选组件，供 GUI **「水印与分享 → 视频裁剪」** 保留原视频音轨时使用。

| 文件 | 说明 |
| ---- | ---- |
| `ffmpeg.exe` | 经 Git LFS 分发（约 84 MB）。克隆后请执行 `git lfs pull`。 |

macOS / Linux 请自行安装系统 `ffmpeg`（或在 PATH 中可用）；程序会优先查找系统 PATH，再回退本目录。

# 🐦 鸟类检测Skill - 更新日志

## v2.0.62 (2026-08-22)

### 主流程

- **切割后再清洗**：勾选清洗后，先从大图检出并切出每只鸟，再对切割图去失焦/无鸟/重复，最后才认种归档。不再清洗 Screened 大图（一张大图里多只鸟时，不在焦平面的个体可以单独去掉）。

---

## v2.0.61 (2026-08-17)

### 主流程

- **预计剩余时间**：连拍按筛选速度估，识别按预计保留张数估，不再用「全目录 × 过慢的单张耗时」。运行中排除首张模型加载、用伪计数把早期实测往先验收，连拍结束后按实际保留张数改后续识别/水印/GPS 预估；连拍快不会去乘识别时间。

---

## v2.0.60 (2026-08-17)

### 连拍筛选

- **快速模式保留比例**：`0.1` 表示本组总张数的约 **10%**。候选集按该保留数（可 2 倍余量）从全组等步长抽取，不再先抽 1/3 再乘比例（否则会变成约 1/30）。

---

## v2.0.59 (2026-08-17)

### 连拍动图

- **后续帧跟踪**：首图标定点后，自动传播改为用鸟体 YOLO 框对准同一只鸟（保持你点在鸟框内的相对位置，如眼/头），并用卡尔曼滤波预测漏检帧；仍找不到再退回原来的模板匹配。定点/跟踪两种模式都走这套。

---

## v2.0.58 (2026-08-17)

### 连拍动图

- **项目文件**：导入图片后在相片目录自动生成与目录同名的 `.birdy-burst.json`，保存图片列表与每帧标定点/裁剪区；关闭窗口后再打开同一文件夹或「打开项目」即可恢复。修改后自动保存，也可「项目另存为」。

---

## v2.0.57 (2026-08-15)

### 连拍动图

- **导出卡住像在做白平衡**：未勾选白平衡时本来就不做白平衡；进度条停在那一句，是因为后面在对**整图**做自动曝光且不刷新进度。现改为裁剪后按帧自动曝光并显示「裁剪并自动曝光 i/n」，未勾选白平衡则直接进入该步骤。

---

## v2.0.56 (2026-08-15)

### 连拍动图

- **导出进度文案**：未勾选「灰世界白平衡」时不再提示「首张统计白平衡，后续沿用」，改为「跳过白平衡」（实际本来就不会做白平衡）。

---

## v2.0.55 (2026-08-15)

### 连拍动图

- **锁定不重跑传播**：在其它页手动指定标定点并锁定后，正在进行或随后完成的传播只**跳过锁定页**，其余页沿用本次传播结果，不会因为锁定而整段重新匹配。

---

## v2.0.54 (2026-08-15)

### 连拍动图

- **传播中途改标定点**：首图标定点与裁剪区确定后，传播尚未完成也可翻到其它页单击指定标定点；绿框立即按首图的相对位置与大小落到新点上，再切换页面时锁定，后台传播不会盖掉。

---

## v2.0.53 (2026-08-15)

### 连拍动图

- **自动曝光即时预览**：勾选「自动曝光」或拖动强度滑条时，右侧当前图立即套用曝光效果，便于对照调整。有裁剪区则按绿框测光，与导出一致。

---

## v2.0.52 (2026-08-15)

### 连拍动图

- **选项参数自动保存**：白平衡、自动曝光、水印、定点/跟踪、帧率、导出尺寸/质量/格式等，关闭后下次打开仍为上次设置（不含图片列表）。

---

## v2.0.51 (2026-08-15)

### 连拍动图

- **自动曝光强度**：滑条最大改为 **3**（默认仍为 **1**）。1=算出的自动曝光；大于 1 在该结果上继续加曝光（2≈+1 档，3≈+2 档）。

---

## v2.0.50 (2026-08-15)

### 连拍动图

- **跟踪模式锁定**：翻页手改标定点后立即锁定，后台传播结束时不再覆盖；状态栏锁定数量随手改更新。

---

## v2.0.49 (2026-08-15)

### 连拍动图

- **自动曝光**：原「生态显影」改为与水印同类的自动曝光；按每帧**裁剪框**测光，并提供曝光强度滑条（0=原图，1=完全调整）。

---

## v2.0.48 (2026-08-15)

### 连拍动图（定点 / 跟踪）

- **放弃 ROI/ECC 对齐**：改为首图标定点 + 裁剪区，后续帧自动传播；越界补边、相对位置不变。
- **定点模式（三脚架）**：模板始终取自首图锚点，搜索同一场景点；**跟踪模式**随上一帧更新模板并用光流初值。
- **设置预览可翻页**：单击标定点、拖拽裁剪区；手工改过的帧锁定，直至「按首图重算后续」。
- **播放帧率**：按每秒几张设置（默认 2），不再用 EXIF 拍照间隔。

---

## v2.0.47 (2026-08-12)

### 图片清洗

- **模糊评分**：先轻度高斯抑噪再算 Laplacian，避免高 ISO 噪点把失焦图打成高清晰度。

---

## v2.0.46 (2026-08-12)

### 图片清洗

- **模糊判定**：画面中有多只鸟时，以**最靠近中央**的鸟体区域计算清晰度（不再取最大框）。

---

## v2.0.45 (2026-08-11)

### 物种识别 · 图片清洗

- **清洗规则**：去除未检出鸟体、鸟体模糊（清晰度滑条）、高度重复（相似度滑条）。
- **单独清洗**：物种卡片内「清洗选定目录」（默认 classification / 分类归档目录）。
- **主流程**：勾选「主流程识别前自动清洗」后，在物种识别前对待识别图片自动执行清洗。

---

## v2.0.44 (2026-08-10)

### 水印生成

- **每物种目录随机抽样**：勾选后按图片父目录（物种文件夹）分组，每组随机抽取可配置张数生成水印；未勾选则全部生成。主流程与「单独批量水印生成」均生效。

---

## v2.0.43 (2026-08-10)

### 轨迹图底图

- **底图风格**：由「数字/卫星」两项改为下拉选择。
- **矢量主题**：标准、幻影黑、月光银、远山黛、草色青、雅士灰、涂鸦、马卡龙、靛青蓝、极夜蓝、酱籽（对齐高德 JS `amap://styles/*` 命名；栅格瓦片上风格化近似）。
- **卫星**：无路网卫星、有路网卫星（影像 + 透明路网注记叠加）。

---

## v2.0.42 (2026-08-10)

### 轨迹图

- **鸟名避让**：上方偏移按鸟图圆半径（像素→pt）+ 字号余量计算，避免压住圆形；行距同步加大。

---

## v2.0.41 (2026-08-10)

### 轨迹图标注微调

- **鸟名**：统一放在鸟图上方。
- **同行间距**：圆心距 = 直径 + 直径×2/5（相对原先再加大 1/5 直径）。
- **定位圆点**：直径放大 100%（更易辨认）。

---

## v2.0.40 (2026-08-10)

### 轨迹图鸟图显示修复

- **根因**：大范围地图上鸟图很大，邻近簇被垂直推挤到画布外（通屏橙色引线、只剩 1 张鸟图）。
- **布局合并**：合并半径 = max(物种去重半径, 约 2.8×鸟图直径对应公里)，优先并成横向网格。
- **取消危险错开**：去掉会推出视野的垂直 nudge；网格优先放在能留在图内的一侧，并对中心做边界钳制；`AnnotationBbox` 关闭裁剪。

---

## v2.0.39 (2026-08-10)

### 轨迹图布局

- **按去重半径合并定位**：鸟图排布不再按精确 GPS 分点，而是与「物种去重半径」相同规则合并邻近位点，再在代表圆点旁做横向网格（每行最多 5 张）。
- 解决大范围地图上因 GPS 微差导致的竖条散点、鸟名挤在圆间的问题。

---

## v2.0.38 (2026-08-10)

### 轨迹图鸟图网格

- **间距**：鸟图圆心距 = 直径 + 直径/5；行距同样含鸟名标注高度，避免堆叠。
- **邻近 GPS 位点**：多个位点鸟图块在垂直方向按行距错开，便于区分。
- **定位线**：每行第一张鸟图与 GPS 圆点用同色（橙色）直线连接，指示该行对应位点。

---

## v2.0.37 (2026-08-10)

### 轨迹图布局

- **GPS 位点鸟图**：同一 GPS 圆点左/右侧横向排列，每行最多 5 张，超出换行；自动选择地图空间较大一侧。
- **鸟名**：位于鸟图上方/下方交错，减少重叠。
- **标题日期**：跨日记录显示起止格式（如 `2026年8月5日-8日`）。
- **标题位置**：优先左上、右上，其次中上，并避让鸟图/鸟名。

---

## v2.0.36 (2026-08-10)

### 修复

- **GUI 启动闪退**：恢复 v2.0.35 重构时误删的 `_track_map_photo_folder`，修复 `AttributeError` 导致窗口无法打开。

---

## v2.0.35 (2026-08-10)

### 轨迹图

- **EXIF GPS 模式**：取消勾选「使用 GPX 轨迹」后，强制从鸟图 EXIF 读取 GPS 绘制标记；生成进度与日志改为「照片 EXIF GPS 模式」，不再提示 GPX 匹配。
- 取消 GPX 时自动启用 EXIF GPS 选项，避免误关「补充 EXIF」导致地图无标注。

### GPS 批量写入

- 按钮更名为 **「批量写入照片 GPS」**。
- 与上方 **写入方式** 一致：**指定地点统一写入** → 使用界面经纬度批量写入；**GPX 按拍摄时间匹配** → 按 GPX 时间插值写入。

---

## v2.0.12 (2026-07-23)

### RAW+JPG 双格式（GUI / CLI）

- **文件夹与输出 → RAW+JPG**：`关` / `仅 JPG` / `JPG + 复制 RAW`。
- **仅 JPG**：连拍筛选、物种归档等主流程只处理 JPEG，忽略同目录 RAW。
- **JPG + 复制 RAW**：主流程仍只处理 JPG；筛选保留后，将同名配对 RAW 复制至 **`Screened_raw_images/`**（与 `Screened_images/` 同级），便于后续人工用 RAW 精修。
- CLI：`--dual-format off|jpg_only|jpg_copy_raw`；逻辑见 **`src/dual_format.py`**。

---

## v2.0.11 (2026-07-23)

### 视频裁剪与稳定（GUI）

- 新增 **`src/video_stabilize_dialog.py`**：主界面「水印与分享」→ **视频裁剪**；支持时间范围截取、空间裁剪、**vidstab** 专业防抖与云台跟随模式；**ffmpeg** 合并原音轨（立体声/多声道）。
- Windows 仓库 **`bin/ffmpeg.exe`**（Git LFS，约 84 MB）；macOS / Linux 使用系统 PATH 中的 ffmpeg。

### 连拍动图对齐

- ROI 对齐由**仅平移**升级为**欧氏变换（旋转 + 平移，±5°）**，改善手持微旋转连拍的对齐质量；链式对齐增加旋转跳变抑制。

---

## v2.0.10 (2026-07-23)

### 轨迹图（GUI）

- **鸟图目录**：启用「输出根目录」且轨迹图未加入主流程时，预览/单独生成能正确解析 `classification_<文件夹名>/` 与 `Screened_images`，不再误读隐藏的手动路径。
- **鸟图来源**：下拉项显示完整自动路径；新增「鸟图目录」浏览覆盖与「当前使用」状态提示。
- **图内标题地点**：优先同步 **水印生成 → 人工地点**（`wm_location_text`），不再沿用「地理位置」里可能过期的 `location_name`。

### 其它

- 观鸟记录导出在未手填分类路径时，同样使用输出根目录推导的 `classification_*` 路径。

---

## v2.0.9 (2026-06-04)

### 观鸟记录导出

- **只数统计修正**：不再按归档裁切文件数量累加；同一原图以 EXIF **拍摄时刻（秒）** 合并，同物种按文件名 **`inst01`/`inst02`** 等区分个体；无 inst 时该原图计 1。时间/空间窗内仍取各原图只数的 **max** 再累加，减轻连拍重复计只。
- **单次活动单文件**：默认整次扫描合并为 **一份** eBird Checklist `.csv` 与 **一份** 观鸟记录中心 `.xls`（连日同点一次观鸟）。
- **定点观鸟去重**：空间模式下 GPS 微移时，**距离或时间窗** 满足即可合并为同一批个体（默认个体时间窗 **120 分钟**，可在 GUI 设置）。

### GUI

- 观鸟记录导出成功弹窗提供 **打开 eBird CSV / 打开 Excel / 打开导出目录** 链接（与轨迹图「打开图片」一致）。
- **累计只数（计数）** 旁 **设置** 可自定义分窗时间（EXIF 拍摄时间）与分窗距离（GPS）。

### 文档

- 新增 **`docs/Birdy-一页使用说明.html`** 一页上手说明；README / 安装说明同步 v2.0.9。

---

## v2.0.8 (2026-06-03)

### 观鸟轨迹图

- **预览与导出一致**：统一 **1440×2560** 画布与鸟图比例（直径 = 图高 **1/32**），避免预览小、导出鸟图过大。
- **地图物种名**：字号改为图高 **1/100**；距左右边界 ≥ 图宽 **1/50**；鸟名与缩略图间距收紧。
- **海拔剖面**：重算 `xlim/ylim`（上留白约 **18%**、下约 **4%**、右约 **9%**）；「海拔(m)」「里程(km)」改 **axes 坐标** 放置；鸟名布局按扩展后绘图区 + **1/50** 边距；修复布局函数误传 `data_x_max` 导致生成失败。

### GUI

- 主流程仅勾选轨迹图/导出时，状态与统计表不再误报连拍/物种路径。
- 轨迹图保存完成弹窗仅提示 **PNG 路径** 与 **打开图片** 链接（详情仍在右侧日志）。
- 连拍/物种 HTML 报告从 GUI 主流程移除（CLI 保留）；模块标题栏统一为 **「加入主流程」**。

### 独立工具

- `tools/BIRDY-观鸟地图/` 界面样式对齐主程序；运行库经 `sync_runtime.py` 同步上述轨迹图改动。

---

## v2.0.7 (2026-06-02)

### 观鸟轨迹图与 GPX

- **GPX 匹配**：照片与轨迹点按 EXIF 时间与 GPX 时间戳匹配（默认 **30 分钟**窗口）；移除「同日回退」以免窗口外照片误入。
- **行迹 PNG**：高德底图 + 轨迹线 + 圆形鸟图标记；含 **海拔剖面** 与物种名侧标（左/右半图分别左/右对齐，多轮重叠避让）。
- **图内标题与汇总**：鸟图/鸟名布局完成后，标题块（日期 → 地点 →「观鸟记录」→ 签名 Logo）与左下 **物种数** 块再计算位置，优先 **左上 / 右上（右对齐）**，必要时水平微调避让。
- **标记上限**：预览 **40** 张、导出 **150** 张；收集/匹配/截断过程写入日志。
- **独立工具**：`tools/BIRDY-观鸟地图/` 可单独打包分享（`sync_runtime.py` 同步运行库）。

### 观鸟记录导出

- 从分类归档导出 **eBird Checklist .csv** 与 **中国观鸟记录中心** 鸟种导入 **.xls**。
- 批次去重、GPX 空间聚类与里程统计；GUI 可勾选 **「加入主流程」** 在归档后自动导出。

### GUI 与主流程

- 各功能模块改为 **可收起卡片**；标题栏 **「加入主流程」** 勾选（GPS / 水印 / 轨迹图 / 观鸟记录导出等）。
- 轨迹图在 **子进程** 生成，避免 matplotlib 与 Qt 同线程死锁。

### 物种识别与其它

- EfficientNet-B0 本地推理修复；地理物种模式与 eBird 名录对照增强。
- **`models/bird-seg.pt`**、**`bird_iden_res34.pth`** 经 **Git LFS** 纳入仓库，克隆即可使用鸟体检测与 ResNet34 识别（见 README）。

---

## v2.0.6 (2026-05-07)

### GUI 输出路径（会话子目录）

- **输出根目录**：在「文件夹与输出」中填写固定本机路径（如 `D:/birdy/output`），再选择「图片文件夹」；程序按相片目录**最后一级文件夹名**自动生成 **`screened_<名称>/`**（内含 `Screened_images/`、连拍分析 JSON）、**`classification_<名称>/`**（物种归档）、**`reports/`**（连拍与物种 HTML 报告）。配置键 **`output_root_folder`** 写入 `gui_config.json`，本机通常只需设一次。
- **留空输出根目录**时仍使用原「输出文件夹」+「分类归档文件夹」两栏手动路径；开始处理时会校验并创建输出与报告目录。

### 连拍动图 ROI 对齐与其它

- ROI 平移对齐：链式相对上一已对齐帧与首张择优、周期重锚首张、链式突跳改首张、自适应 reproj 阈值与 RANSAC 式位移精化；终端 **`[Birdy 连拍对齐]`** 逐帧日志；调试叠画黄/橙框与字号线宽优化；Windows 副线程 OpenMP 缓解（`KMP_DUPLICATE_LIB_OK` 等）。
- 预览线程与 GUI 槽 **`Qt.QueuedConnection`**，减轻 OMP 与跨线程定时器导致的闪退。

---

## v2.0.5 (2026-05-06)

### 连拍动图 / 视频导出

- **双格式导出**：GUI「动图生成」对话框内可选择 **WebP 动图** 或 **MP4 视频**（`cv2.VideoWriter` + **mp4v**），帧率由推断拍摄间隔与播放倍率换算（约 0.25～120 fps），便于在不支持动图 WebP 的客户端播放。
- **共用处理链**：与 WebP 相同（白平衡、生态显影、对齐、裁剪、缩放、叠水印），`burst_webp.py` 中抽出 `_burst_prepare_animation_pil_frames`，再由 `build_animated_webp` / `build_animated_mp4` 分别写盘；终端日志前缀分别为 `[Birdy WebP导出]`、`[Birdy 视频导出]`。

### 连拍对齐（ROI 刚体）

- **手动 ROI**：预览中黄虚线 **矩形** 拖拽框选参考物；仅在 ROI 掩膜内提特征并估计 **平移 + 小旋转**，整图 `warpAffine` + 常量边界留白；未设 ROI 时回退 **边带相位/ECC** 自动对齐。
- **检测器**：ORB / SIFT / BRISK（SIFT 不可用时回退 ORB）；调试叠加与 ROI 语义一致。

### 其它

- 对话框窗口标题与主界面「动图生成」工具提示已写明 WebP / MP4；状态 JSON 增加 **`export_format`**（`webp` | `mp4`）。

---

## v2.0.4 (2026-05-04)

### 水印与自动显影

- **批量色调滑条下线**：不再提供水印前的「暗部 / 曝光 / 对比度」三滑条批量增强。
- **自动生态显影**：水印前处理与 RAW 筛选复制时所用 **`ecology_jpeg_develop`** 显影栈对齐，默认开启；可选关闭。
- **逐张微调与临时配置**：新增 **`src/watermark_enhance.py`**，预览内支持「自动强度」「曝光微调」、**本张恢复默认**、上一张/下一张切换前自动保存；参数写入系统临时目录下的 **`overrides.json`**（按水印输入目录路径哈希分桶），主流程批量水印自动读取。
- **GUI 配置**：使用 **`wm_enable_auto_enhance`**；旧配置仅有 **`wm_enable_tone_adjust`** 时，加载后默认启用新自动显影。

---

## v2.0.3 (2026-05-02)

### GPS 与连拍筛选

- **GPS 写入时机调整**：仅在连拍筛选完成并将保留图复制到输出目录下的 **`Screened_images`** 之后，再对该目录（含子目录）内的 **JPEG** 递归写入 GPS；**不再**对原始输入目录批量写 EXIF。
- **物种与地理约束**：连拍流程成功后，物种/归档阶段默认使用 **`screened_paths_for_kept_images`** 解析出的 **Screened 副本路径**（与复制规则一致，含 RAW→同路径 `.jpg`），使 `detect()` 内 `gps_to_location_meta` 能读到刚写入的 GPS；若副本缺失则回退为原 `get_kept_images` 路径。界面省市仍可作为无 GPS 时的手工地理参考。

### RAW 与图像管线

- 新增 **`src/image_io.py`**：集中常见相机 RAW 扩展名与 `rawpy` 解码；水印打开、连拍鸟检/对焦/鸟眼等统一经 `imread_bgr` / `open_pil_rgb`。
- 新增 **`src/ecology_jpeg_develop.py`**：筛选复制 RAW 时做生态向显影（整体明暗、CLAHE、暗部提亮、自适应降噪与锐化）再导出 JPEG。
- **`requirements.txt`**：将 **rawpy** 列为正式依赖。

### 水印与 GUI

- 独立「批量水印生成」改为 **后台线程**，接进度与日志，避免界面卡死。
- 水印前可选 **色调调整**（暗部提亮、曝光、对比度），预览对话框支持滑条实时刷新、**上一张/下一张**、确定后写回配置。

### 其它

- **`geo_encoder`**：`batch_write_gps_exif` 改为 `rglob` 递归；`write_gps_exif` 对非 JPEG 直接跳过，避免格式误写。

---

## v2.0.2 (2026-04-17)

### 产品定位与许可说明

- README 介绍重写：定位为拍鸟场景下的海量相片自动化整理与归档工具，并按连拍筛选、检测、识别归档、地理写入、水印与报告等流程组织核心能力。
- 许可说明统一为：仅限爱好者、公益、科研等非盈利用途，请勿用于商业用途。

---

## v2.0.1 (2026-04-17)

### 仓库元数据

- 明确 **`models/bird_info.json`** 为应纳入版本控制的索引文件；**`.gitignore`** 不再保留「可忽略 bird_info」类表述，避免误配。

---

## v2.0.0 (2026-04-17) 🎉

### 稳定版发布

- 阶段性功能迭代收敛，发布 **2.0.0** 作为当前稳定基线版本；**GitHub 公开发布日期：2026-04-17**（与 `version-info.json` 的 `release_date` 一致）。

### 物种识别与地理约束

- 本地物种识别地理规则升级为 **top10 地理筛选**：
  - 在地理名单内（省级或中国索引）按规则优先；
  - 不在地理名单的候选仅当 **`confidence > 0.75`** 才保留。
- 新增可解释日志，未知判定会输出 Top1 与过滤原因，便于参数调优与排查。

### 阈值策略（GUI）

- GUI 的「未知种类阈值」改为**可选开关**：
  - 默认不启用：不做 top10 置信度阈值初筛，仅按地理规则筛选；
  - 启用后：按阈值进行候选过滤并执行低置信度未知判定。

### 豆包 API 优化

- 请求侧禁用 thinking/reasoning 输出，减少不必要 token 开销。
- 识别结果简化为「鸟类 / 其它」，非鸟统一归档为「其它」。
- 404/401/403 场景下，失效模型会话内自动加入黑名单，后续调用会跳过，避免反复命中同一坏模型。

### 水印输出

- 水印批处理输出不再镜像原目录层级，统一直接保存到目标输出目录根下；同名文件自动追加序号避让。

---

## v1.2.0 (2026-04-15) 🚀

### 鸟眼检测召回增强

- 鸟眼检测由整图推理改为**逐鸟体 ROI 推理**，并支持 ROI 外扩参数，提升小目标与远距离场景召回。
- 有效眼判定升级为“眼中心落入鸟体框 + 容差比例”，降低框偏移导致的漏判。
- 新增每鸟高置信 TopN 保留策略，减少噪声框干扰。

### 连拍评分与筛选优化

- 连拍综合分引入 **`BIRD_AREA_WEIGHT`**，将鸟体面积项改为可配置权重并降低默认值，减少“大鸟体”对排序的过度主导。
- 鸟眼检出继续以加权项参与筛选，不破坏原有清晰度主逻辑。

### 进度条与耗时感知优化

- GUI 进度条从“按步骤跳变”升级为“按阶段内真实处理量推进”：
  - 连拍阶段按已处理图片/总图片实时推进；
  - 物种阶段按逐图处理实时推进；
  - 水印阶段按逐图生成实时推进。
- 预计剩余时间与用户体感一致性提升，长阶段不再出现“长时间卡住不动”。

### 新增水印功能卡片

- 增加「水印生成」卡片，支持输入/输出目录、签名 Logo、地理位置（手填或 GPS 城市）、日期、物种名、相机参数等选项。
- 新增「预览一张效果」按钮，支持先预览再批量生成。

### 目录结构规范化

- `data/`、`models/`、`test/` 调整到项目根目录与 `src/` 同级。
- logo 等静态资源统一归档到 `resources/`，并同步修正代码路径与文档说明。

---

## v1.1.10 (2026-04-12) 🐛

### 经纬度输入改为数字文本框

- **GUI 经纬度输入**：`QDoubleSpinBox` → `QLineEdit`（避免鼠标滚轮误触改变数值）
- 新增 `_get_gps_coords()` 辅助方法，支持范围校验（纬度 -90~90，经度 -180~180），非法输入弹出警告并阻断处理流程。
- 输入框占位提示：`"例: 31.230416"` / `"例: 121.473682"`

### 本地模型与豆包 API 完全解耦

- **本地模型模式**：不再读取 `doubao_api_config.json`，彻底消除不必要的文件 I/O 和配置依赖。
- **豆包 API 模式**：同样不再强制加载本地权重配置。

### 本地模型「未知种类」阈值可配置

- 新增 `min_species_accept_confidence` 参数（默认 **0.5**，低于此值视为未知）。
- **GUI**：物种识别卡片新增「未知种类阈值」旋钮（0.0~1.0，步进 0.05）。
- **CLI**：新增 `--min-species-conf` 参数（例：`--min-species-conf 0.3`）。
- 此参数**仅对本地模型生效**，豆包 API 模式不受影响。
- 旧版硬编码的 0.7 阈值已废除。

---

## v1.1.9 (2026-04-11) ✨

### 连拍输出

- 连拍筛选**保留**的图片复制到 **`{output_folder}/Screened_images/`**，并尽量保持相对输入目录的子路径，避免重名覆盖。

### 物种置信度与「未知」

- 顶一物种候选**置信度低于 70%**（或地理过滤后无候选）视为**未知种类**，分类为 **未知目 / 未知科 / 未知属 / 未知**，裁剪或原图归档均归入该分级目录。

### 物种识别与裁剪拆分（GUI / CLI）

- 配置项 **`enable_species_detection`** 与 **`enable_crop`** 独立：可只做识别不归档裁剪、或只裁剪不跑物种模型等组合。
- **仅识别、不勾选裁剪**：将**整张原图**复制到「裁剪输出文件夹」下，按**置信度最高的物种**对应 目/科/属/种 目录（多鸟取全局最高置信度候选）；低于 70% 走未知分级。
- CLI：`--no-species` 禁用物种识别；`--no-crop` 关闭裁剪（保留物种时走原图复制）。

---

## v1.1.8 (2026-04-11) 🔧

### 模型路径与报错说明

- **`BirdAndEyeDetector` 默认权重路径**：统一到项目根 **`models/`**（与 `src/` 同级）。从项目根执行 `python src/birdy_gui.py` 或 `start_gui.bat` 时会正确加载 `models/bird-seg.pt` 等，避免误读错误/空文件导致 `PytorchStreamReader … zip … central directory` 一类错误。
- **加载失败提示**：对 YOLO 与 `torch.load` 物种权重捕获上述 zip/损坏类异常，抛出带中文说明的 `RuntimeError`（含绝对路径与排查建议）。

---

## v1.1.7 (2026-04-11) 🔧

### Windows 任务栏图标

- **GUI 入口**：在创建 `QApplication` 前调用 `SetCurrentProcessExplicitAppUserModelID`，使 Birdy 与宿主 `python.exe`（如 Anaconda/Jupyter 共用解释器时）在任务栏上分离，从而采用 `setWindowIcon` 的 Birdy Logo。
- 创建主窗口后对 `QApplication.setWindowIcon` 同步主窗图标，提高壳层识别一致性。
- **`start_gui.bat`**：补充说明若仍异常可检查 `resources\birdy_logo_128.png` 或使用快捷方式指定 `.ico`。

---

## v1.1.6 (2026-04-11) ✨

- **GUI**：处理进度卡片在进度条下方增加 **已用时间**（`time.monotonic()` + 500ms 刷新）与 **预计剩余**（按当前进度百分比线性估算：已用 × (100−p) / p；p=0 时显示「—」，完成时「0秒」）。

---

## v1.1.5 (2026-04-11) 🔧

- **GUI**：整体收紧间距与内边距（主区、卡片、表单、全局 QSS、顶栏、进度区），控件更紧凑。
- **默认模型**：`gui_config.json` 中 `use_local_model` 改为 `true`；若旧配置缺少该字段，加载时默认本地模型。

---

## v1.1.4 (2026-04-11) ✨

### 品牌与界面（GUI）

- **产品名称**：中文「鸟图智慧仓储」，英文「Birdy」；窗口标题栏显示 **Birdy**。
- **固定顶栏**：顶部白底品牌栏展示 Logo、中英文名称与版本号（版本读取 `version-info.json`），与下方功能区分离，**不参与左右侧滚动**。
- **图标**：窗口与 Windows 任务栏图标优先使用 `resources/birdy_logo_128.png`，其次 `birdy_logo_640.png` / `logo.png`；无文件时沿用内置矢量回退绘制。
- **Banner Logo**：优先 `resources/birdy_logo_640.png`，其次 `birdy_logo_128.png` / `logo.png`。
- 右侧底部重复大图 Logo 已移除（改由顶栏统一展示）。

---

## v1.1.3 (2026-04-11) 🔧

### GUI 布局

- **可滚动面板**：左右两栏内容分别放入 `QScrollArea`（`setWidgetResizable(True)`），内容按自然高度排版；窗口高度不足时纵向滚动，避免 `QVBoxLayout` 把输入框、按钮压扁导致裁切。
- **尺寸策略**：设置面板与卡片使用 `QSizePolicy` 纵向 `Minimum`，优先占满内容所需高度而非被强行压缩。

---

## v1.1.2 (2026-04-11) 🔧

### GUI 显示

- **高 DPI / 缩放**：在创建 `QApplication` 前启用 `AA_EnableHighDpiScaling` 与 `AA_UseHighDpiPixmaps`，改善 Windows 在 125%/150%/200% 缩放下界面与字体过小的问题。
- **字体与样式表**：全局样式由 `px` 改为 `pt`，并略增大正文字号；卡片标题、主副标题、按钮、日志与表格统一在 QSS 中指定字号，避免仅改 `QFont` 被样式表覆盖而不生效。
- **控件与资源**：复选框/单选框指示器加大；日志等宽字体补充 `Microsoft YaHei UI` 回退；底部 logo 按主屏 `devicePixelRatio` 缩放并设置 pixmap DPR，高分屏上更清晰。

---

## v1.1.1 (2026-03-19) 🔧

### 修复与改进

#### 1. 依赖库问题修复 ⭐

- **PyQt5 替换 PyQt6**
  - 将 `requirements.txt` 中的 PyQt6 改为 PyQt5>=5.15.0
  - 与实际代码使用的 PyQt5 保持一致
  - 移除了不必要的 timm 依赖

#### 2. 文件结构标准化 ⭐

- **源文件组织**
  - 所有 Python 源代码移至 `src/` 目录
  - 模型文件移至 `models/` 目录
  - 数据资源移至 `data/` 目录
- **启动脚本**
  - `start_gui.bat` - Windows GUI 启动脚本
  - `start_gui.sh` - Linux/macOS GUI 启动脚本

#### 3. 文件重命名 ⭐

- **GUI 主程序**
  - 从 `gui_app.py` 重命名为 `birdy_gui.py`
  - 保持 PyQt5 框架不变
- **CLI 主程序**
  - 新增 `birdy_cli.py` - 命令行界面
  - 支持完整的参数配置和批处理

#### 4. 文档完善 ⭐

- **README.md**
  - 完整的项目介绍和功能说明
  - 详细的目录结构说明
  - 快速开始指南和参数说明
  - 工作流程和API集成说明
- **skill.md**
  - 更新所有路径引用到新结构
  - 添加 CLI 版本使用说明
  - 更新文件结构和API示例
- **version-info.json**
  - 版本号升级到 1.1.1
  - 更新发布日期到 2026-03-19
  - 添加 v1.1.1 的变更日志

### 技术细节

#### 路径引用更新

所有代码中的路径引用已更新：

```python
# 旧路径
from gui_app import ...
from detect_bird_and_eye import ...

# 新路径（保持相对导入）
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
from burst_grouping import process_folder
from html_report_generator import generate_html_report
```

#### 模型路径

```python
# 模型文件在 models/
models/bird-seg.pt
models/birdeye.pt
models/bird-iden.pth
models/bird_info.json
```

#### 数据路径

```python
# 数据文件在 data/
data/species/bird_classification.json
data/species/geo_species_index.json
data/species/species_text_aliases.json
data/geo/world-area.json
```

### 文件变更

#### 新增文件 ⭐

```
birdy-skill-1.1.1/
├── README.md                  ✨ 新增完整README
├── src/                       ✨ 新增源文件目录
│   ├── birdy_gui.py          ⭐ 重命名（原gui_app.py）
│   ├── birdy_cli.py          ⭐ 新增CLI主程序
├── models/                   ✨ 新增模型目录
├── data/                     ✨ 新增数据目录
├── resources/                ✨ 新增资源目录
├── test/                     ✨ 新增测试目录
├── start_gui.bat             ✨ 新增Windows启动脚本
└── start_gui.sh              ✨ 新增Linux/macOS启动脚本
```

#### 修改文件 🔧

```
├── requirements.txt           🔧 PyQt6→PyQt5，移除timm
├── skill.md                  🔧 更新路径和版本
├── version-info.json             🔧 版本号→1.1.1
└── CHANGELOG.md              🔧 添加v1.1.1日志
```

### 向后兼容性

✅ **完全兼容**

- 所有原有API保持不变
- 命令行脚本继续工作
- Python代码集成无影响
- 配置文件格式相同
- 只需更新启动脚本路径

### 测试覆盖

- ✅ GUI启动和加载
- ✅ CLI命令行处理
- ✅ 参数配置保存
- ✅ GPS查询功能
- ✅ 工作线程处理
- ✅ 异常处理机制
- ✅ 跨平台兼容性
- ✅ EXIF信息保留


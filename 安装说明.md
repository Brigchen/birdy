# 鸟图智慧仓储（Birdy）安装与分发说明

版本号与发布日期以项目根目录 `**version-info.json**` 中的 `version`、`release_date` 为准（发版时会与 **README.md**、**CHANGELOG.md** 对齐）。

## 一、环境要求


| 项目     | 建议                                      |
| ------ | --------------------------------------- |
| 操作系统   | Windows 10/11、macOS、Linux               |
| Python | **3.8～3.12**（3.13 可能可用，请以实际测试为准）        |
| 内存     | ≥ 8 GB（处理大批量连拍时更宽裕）                     |
| 磁盘     | ≥ 3 GB（含依赖；`bird-seg.pt` 与 `bird_iden_res34.pth` 已随仓库 Git LFS 分发，克隆后请 `git lfs pull`） |
| GPU    | 可选；有 NVIDIA GPU 时可安装 CUDA 版 PyTorch 加速  |


## 二、获取代码与模型

1. 解压或克隆本仓库到本地，**工作目录应为包含 `src/`、`requirements.txt` 的根目录**。
2. **Git LFS**：安装 [Git LFS](https://git-lfs.com/) 后，在仓库根目录执行 `git lfs pull`，拉取 `models/bird-seg.pt` 与 `models/bird_iden_res34.pth`（单文件 >100 MB，经 LFS 托管）。
3. **模型文件**应位于 `**models/**`（与 `src/` 同级）。仓库已含：
  - `bird-seg.pt` — 鸟体检测（LFS）  
  - `bird_iden_res34.pth`、`bird_info.json` — 本地物种分类 ResNet34（LFS + JSON）  
  可选、需自备：  
  - `bird_iden_efficient_b0.pt` — EfficientNet-B0 物种分类  
  - `birdeye.pt` — 鸟眼检测（若启用鸟眼功能）
4. `data/species/bird_species_list.csv` 为中英名称与学名对照清单（由 `bird_classification.json` 整理），用于名称差异时的统一规范与检索；建议保留随项目分发。
5. 若出现「zip / central directory」类报错，多为 LFS 未拉取完整或权重损坏，请执行 `git lfs pull` 并核对文件大小（`bird-seg.pt` 约 88 MB，`bird_iden_res34.pth` 约 103 MB）。

## 三、安装依赖

在**项目根目录**打开终端：

```bash
python -m venv .venv
```

**Windows：**

```bat
.venv\Scripts\activate
pip install -U pip
pip install -r requirements.txt
```

**macOS / Linux：**

```bash
source .venv/bin/activate
pip install -U pip
pip install -r requirements.txt
```

**GPU 用户**：可到 [PyTorch 官网](https://pytorch.org) 选择 CUDA 版本安装 `torch`、`torchvision`，再执行：

```bash
pip install -r requirements.txt --no-deps
pip install opencv-python Pillow ultralytics numpy PyQt5 piexif geopy requests tqdm xlrd xlwt xlutils matplotlib
```

（或先注释掉 `requirements.txt` 中的 `torch` / `torchvision` 两行后只装其余包。）

## 四、配置文件（可选但推荐）


| 文件                               | 作用                                                                                                    |
| -------------------------------- | ----------------------------------------------------------------------------------------------------- |
| `**src/doubao_api_config.json**` | 火山方舟豆包视觉 API：`api_key`、`api_base`、`models` 多模型轮换、`daily_token_limit_per_model`、`token_switch_ratio` 等 |
| `**src/amap_api_config.json**`   | 高德 Web 服务 Key；地名转坐标（GUI「高德API → 打开配置文件」）                                                              |
| `**src/geocoding_config.py**`    | 地理编码回退与其它地图开关（JSON 未填 Key 时可在此填写）                                                                     |
| `**src/gui_config.json**`        | GUI 保存的界面参数（运行后自动生成/更新）                                                                               |


将仓库分享给他人时，**请勿**把真实 API Key 一并公开。根目录 `**.gitignore`** 已忽略 `doubao_api_config.json`、`amap_api_config.json` 等；克隆后若缺少这些文件，首次打开 GUI 中的「豆包 / 高德」配置或运行相关流程时，程序会在 `src/` 下**自动生成**空密钥的 JSON 模板（见 `src/api_config_defaults.py`）。

## 五、启动方式

**务必在项目根目录执行**，或直接使用启动脚本（脚本会 `cd` 到 `src` 再运行）。

### Windows

双击 `**start_gui.bat`**，或在根目录执行：

```bat
python src\birdy_gui.py
```

### macOS / Linux

```bash
chmod +x start_gui.sh
./start_gui.sh
```

或：

```bash
python src/birdy_gui.py
```

### 命令行（批处理）

```bash
python src/birdy_cli.py -i ./你的图片文件夹 -o ./outputs
python src/birdy_cli.py --help
```

## 六、首次运行建议流程

1. **依赖与模型**：完成第三节 `pip install` 后，在仓库根目录执行 **`git lfs pull`**，确认 `models/bird-seg.pt`（约 88 MB）、`models/bird_iden_res34.pth`（约 103 MB）大小正常。
2. **API Key（按需填写）**  
   - **不配置任何 Key** 即可使用：连拍筛选、鸟体检测、**ResNet34 本地物种识别**、指定地点统一 GPS 写入（默认上海坐标）。  
   - **高德 Key**：GUI「地理位置」→「打开配置文件」编辑 `src/amap_api_config.json`（地名反查、水印地点、**主程序轨迹图高德底图**）。  
   - **豆包 Key**：仅在选用豆包物种识别时编辑 `src/doubao_api_config.json`。  
   - 配置文件首次打开时会自动创建空模板，填入个人申请的 Key 即可。
3. 填写 **输出根目录**（推荐）或「输出文件夹」+「分类归档文件夹」；再选择**图片文件夹**（自备相片，仓库不含样例图）。
4. 若使用连拍筛选：勾选「连拍检测」；保留结果会写入 **`{screened_会话}/Screened_images/`**。
5. **GPX 轨迹**为可选：仅在「GPX 按拍摄时间写入 GPS」或「轨迹图生成」时需要**用户自备** GPX 文件；仓库 `data/gpx/` 仅含工具脚本说明，不含个人轨迹数据。
6. **连拍动图 / 视频**：主界面「水印与分享」→「动图生成」（WebP / MP4）。
7. **观鸟行迹地图**：主程序轨迹图需高德 Key；或使用独立工具 `tools/BIRDY-观鸟地图/`（网格底图，无需 Key，自备 GPX + 鸟图）。
8. **观鸟记录导出**：完成物种归档后，在「观鸟记录导出」卡片导出 eBird / 观鸟记录中心表格（默认写入 `reports/`）；导出完成后弹窗可打开文件。只数按原图拍摄时刻与裁切文件名 `inst` 统计，上传前请人工核对。一页说明见 `docs/Birdy-一页使用说明.html`。

## 七、分发与共享清单

向他人分享时，可直接 **`git clone`** 本仓库（含 Git LFS 权重）或打包源码树，并说明：

- `requirements.txt`、`README.md`、本 **`安装说明.md`**、`docs/Birdy-一页使用说明.html`、`version-info.json`  
- 克隆后执行 **`git lfs pull`**  
- **勿**提交或分享含真实 Key 的 `src/*_api_config.json`、`src/gui_config.json`  
- 个人 GPX、相片、输出目录由使用者自备（`.gitignore` 已排除）

## 八、许可与声明

- 项目整体许可见仓库中的 **LICENSE**（若存在）。  
- **Ultralytics YOLOv8** 采用 **AGPL-3.0**。本项目整体仅限爱好者、公益、科研等非盈利用途，请勿用于商业用途。  
- 具体使用边界与第三方组件许可请以仓库 README、LICENSE 与各依赖官方许可证文本为准。

## 九、问题排查


| 现象                      | 处理方向                                                    |
| ----------------------- | ------------------------------------------------------- |
| `No module named PyQt5` | 在已激活的 venv 中执行 `pip install -r requirements.txt`        |
| 找不到模型 / zip 报错          | 执行 **`git lfs pull`**；确认 `bird-seg.pt`≈88 MB、`bird_iden_res34.pth`≈103 MB |
| 轨迹图提示高德 Key 未配置        | 编辑 `src/amap_api_config.json` 填写 `api_key`，或使用 `tools/BIRDY-观鸟地图/`（无需 Key） |
| 豆包 404                  | 检查 `api_base` 与控制台 **推理接入点 ID**（`model` / `models`）是否一致 |
| 连拍后物种步骤无图               | 未勾选连拍时，需先有 `输出目录/Screened_images` 中的筛选图                 |
| MP4 导出失败 / 无法打开写入器     | 确认输出路径以 `.mp4` 结尾、目录可写；在 venv 中重装 `opencv-python`；部分环境需本机 ffmpeg 可被 OpenCV 调用 |


更多技术细节见 `**README.md**`。
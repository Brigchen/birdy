# Birdy / 鸟图智慧仓储 — Python 依赖
# 安装: pip install -r requirements.txt
#
# PyTorch：若使用 NVIDIA GPU，建议到 https://pytorch.org 按 CUDA 版本安装
# torch / torchvision，再安装本文件其余依赖（可注释掉下面两行后单独安装）。

# 深度学习 / 推理
torch>=1.9.0
torchvision>=0.10.0
ultralytics>=8.0.0

# AI 图像增强（水印前降噪/锐化；可选，未安装时自动降级跳过）
einops>=0.6.0
basicsr>=1.4.0
realesrgan>=0.3.0

# 图像与数值
opencv-python>=4.5.0
Pillow>=8.0.0
numpy>=1.19.0

# 图形界面
PyQt5>=5.15.0

# 网络与地理编码 / EXIF
requests>=2.25.0
piexif>=1.1.3
geopy>=2.0.0
pypinyin>=0.49.0
openpyxl>=3.0.0

# 其它
tqdm>=4.60.0

# 中国观鸟记录中心鸟种导入（.xls 模版复制写入）
xlrd>=2.0.1
xlwt>=1.3.0
xlutils>=2.0.0

# 轨迹图 PNG 导出（高德底图瓦片需联网 + amap_api_config.json）
matplotlib>=3.5.0

# RAW（常见相机）；未安装时扩展名仍会被识别，但无法解码
rawpy>=0.16.0

# 视频裁剪与稳定（GUI「视频裁剪」；未安装时回退内置 OpenCV 算法）
vidstab>=1.7.0
